services:
  openjenie:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env.local
    ports:
      - "3000:3000"
    volumes:
      - ./data:/data
    restart: unless-stopped
